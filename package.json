{
  "name": "crear_plan_estudio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"next dev --turbopack\" \"uvicorn src.server.server:app --reload --host 127.0.0.1 --port 8000\"",
    "build": "next build",
    "start": "concurrently \"next start --turbopack\" \"uvicorn src.server.server:app --reload --host 127.0.0.1 --port 8000\"",
    "lint": "next lint"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.6",
    "next": "15.3.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "concurrently": "^9.1.2",
    "tailwindcss": "^4"
  }
}
